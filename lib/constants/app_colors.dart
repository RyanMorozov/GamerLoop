import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF0D0D0D);
  static const Color accent = Color(0xFF00FF95);
  static const Color textPrimary = Colors.white;
}